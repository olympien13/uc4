---

- name: Get pet by ID
  ansible.builtin.uri:
    url: "{{ pet_endpoint }}/{{ pet_id }}"
    method: GET
    headers:
      Content-Type: "application/json"
    return_content: yes
  register: result

- name: Debug result
  debug:
    var: result
